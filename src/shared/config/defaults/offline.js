AONC.define('config.defaults.offline', function () {
  return {
    // Заменять страницу ошибки браузера своей плашкой «включите VPN / другое зеркало»
    autoPage: true,
    // Плашка внутри страницы, если сайт открылся, но пустой или заблокирован
    siteBanner: true,
    // Опрашивать зеркала, чтобы предлагать то, которое реально отвечает
    probe: true,
    // Таймаут одного запроса к зеркалу
    probeTimeoutMs: 7000,
    // Сколько ждём отрисовку, прежде чем считать страницу «пустой»
    emptyPageMs: 4500,
    // Меньше этого количества текста + нет ссылок/картинок = страница не открылась
    minTextLength: 40
  };
});
